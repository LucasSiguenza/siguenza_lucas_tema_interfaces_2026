import { DuracionMinutosPipe } from './duracion-minutos-pipe';

describe('DuracionMinutosPipe', () => {
  it('create an instance', () => {
    const pipe = new DuracionMinutosPipe();
    expect(pipe).toBeTruthy();
  });
});
